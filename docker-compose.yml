name: next_template
services:
  next_template:
    container_name: next_template
    image: next_template:dev
    build:
      context: .
      target: base
      dockerfile: ./Dockerfile
    command: bash -c "npm install && npm run dev"
    restart: always
    env_file: .env
    environment:
      - PORT=${FRONT_PORT}
    ports:
      - ${FRONT_PORT}:${FRONT_PORT}
    volumes:
      - ./frontend:/app
